{
  "frontend/src/App.tsx": "Main application component with TanStack Router; adds the `/ageing-stock` route pointing to the new AgeingStock page alongside all existing routes.",
  "frontend/src/components/layout/AppSidebar.tsx": "Left sidebar navigation with the new \"Ageing Stock\" menu item (Clock icon, `/ageing-stock` path) added after Reconciliation; all other items and the Data Reset button are unchanged.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page using named imports `{ TotalOrdersTab }` and `{ HallmarkTab }` to match the updated component exports, passing `allOrders` and `isError` props to each tab.",
  "frontend/src/components/dashboard/SummaryCards.tsx": "Summary cards that compute metrics per tab; the Total Orders tab now excludes ghost Pending entries (Pending rows whose orderId also has a Ready entry) from all calculations, so fully-submitted split RB lines do not contribute to order count, weight, or quantity totals.",
  "frontend/src/components/dashboard/TotalOrdersTab.tsx": "Total Orders tab with grouped expandable rows; fixes invisible content by replacing `bg-card`/`text-foreground` CSS variable classes with explicit `bg-zinc-900`/`text-white` Tailwind classes, and always prefers live karigar/generic names from design mappings over stale order data.",
  "frontend/src/components/dashboard/ReadyTab.tsx": "Ready tab with grouped expandable rows; fixes invisible content with explicit `bg-zinc-900`/`text-white` classes, adds visible Excel/PDF/JPEG export buttons in the toolbar, and includes bulk \"Mark as Pending\" action for selected orders.",
  "frontend/src/components/dashboard/HallmarkTab.tsx": "Hallmark tab rebuilt with the same grouped card layout (replacing the Table component) using explicit `bg-zinc-900`/`text-white` classes to fix invisible content, retaining export buttons, bulk mark-as-returned, and design image modal.",
  "frontend/src/components/dashboard/CustomerOrdersTab.tsx": "Tab displaying CO type orders with Pending status using search by order number, design code, or generic name.",
  "frontend/src/components/dashboard/KarigarsTab.tsx": "Karigar management tab with explicit `bg-zinc-900`/`text-white` classes to fix invisible card content; always uses live design mapping karigar names for grouping, and navigates to `/karigar/$name` with URL-encoded karigar name on card click.",
  "frontend/src/components/dashboard/OrderTable.tsx": "Reusable order table with bulk selection, mark-as-ready, and export dropdown; `exportToPDF`/`exportToJPEG` calls no longer pass the actor argument since the updated exportUtils no longer requires it.",
  "frontend/src/pages/IngestOrders.tsx": "Order ingestion page that uses the existing `parseOrdersExcel` utility (which returns a typed array directly) to parse Excel files, generates unique orderIds with orderNo+design+timestamp+index+random suffix to prevent duplicate key traps, converts orderDate ms to nanoseconds BigInt, processes orders sequentially, and displays clean error messages extracted from IC canister rejections.",
  "frontend/src/pages/UnmappedCodes.tsx": "Unmapped orders page with inline editing; replaces removed `useSaveDesignMapping` import with `useUpdateDesignMapping` and imports `useGetUniqueKarigarsFromDesignMappings` from the corrected hooks file.",
  "frontend/src/pages/MasterDesigns.tsx": "Master designs page; removes the `availableKarigars`, `onSave`, and `onAddKarigar` props from `EditDesignModal` usage since the modal now self-manages karigar loading and saving internally.",
  "frontend/src/pages/DesignImages.tsx": "Design image upload page with optimized parallel upload processing (5 concurrent uploads), support for both folder and ZIP file uploads with proper type conversion for Uint8Array to ensure ArrayBuffer compatibility, progress tracking with current file display, automatic design code extraction from filenames, and detailed upload result reporting using ExternalBlob for backend storage.",
  "frontend/src/pages/TagPrinting.tsx": "Tag printing page with date-range filtering and design-group selection; updated to use `useGetReadyOrdersByDateRange` as a mutation (called imperatively on button click) and imports `useUpdateDesignGroupStatus` correctly.",
  "frontend/src/pages/BarcodeScanning.tsx": "Barcode scanning page with dual-mode support for hardware scanners and camera-based scanning using useQRScanner hook, automatic barcode detection with debouncing, enhanced visual feedback with gold targeting frame overlay and success animations, proper camera initialization state handling with jsQRLoaded check, automatic order ID extraction from scanned data, and batch update functionality using useBatchUpdateOrderStatus hook.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file; restores `useGetUnmappedOrders` and `useGetOrdersByKarigar`, fixes `useBatchSupplyRBOrders` to accept `{ orderId, suppliedQty: number }[]` (converting to BigInt internally), and ensures all mutation hooks invalidate relevant query caches on success.",
  "frontend/src/utils/excelParser.ts": "Excel parsing utilities that scan row 1 headers for an 'Order Type' column (normalized, case-insensitive, ignoring spaces/underscores); if found, each data row's Order Type is read from that column and resolved to RB/SO/CO (blank or unrecognised defaults to CO); if no 'Order Type' header exists, all orders default to CO for backward compatibility. Also removes the validation error for missing/unrecognised Order Type since CO is now the valid default fallback.",
  "frontend/src/utils/exportUtils.ts": "Export utilities for CSV/Excel, PDF (jsPDF CDN), and JPEG (canvas); `exportToPDF` and `exportToJPEG` now take `(orders, filename?)` with no actor parameter, and adds `exportKarigarToPDF` alias for backward compatibility with KarigarDetail.",
  "frontend/src/index.css": "Global styles with OKLCH color system; fixes dark mode by raising `--muted`, `--card`, `--background`, and `--border` lightness values so skeleton/loading states are visible against the dark background. Removes the problematic `[data-state=\"active\"]` global color override and the `button:hover` scale transform that were causing invisible text and layout issues.",
  "frontend/tailwind.config.js": "Tailwind configuration with Inter and Playfair Display fonts, gold color tokens with hover variants, enhanced shadows including gold-themed shadows, and responsive container padding for mobile-first jewellery industry design.",
  "frontend/src/components/dashboard/DesignImageModal.tsx": "Modal for displaying design images with zoom/pan controls; fixed to derive `imageUrl` from `designData.blob.getDirectURL()` and use the `designCode` prop directly for the design details section, matching the updated `useGetDesignImage` hook return shape of `{ blob, genericName, karigarName }`.",
  "frontend/src/components/dashboard/UnmappedSection.tsx": "Alert section showing unmapped design codes; adds explicit `UnmappedGroup` interface and typed `reduce` accumulator to fix all TypeScript `unknown` type errors.",
  "frontend/src/components/tagprinting/WebviewModal.tsx": "Reusable webview modal component displaying MPN system in an iframe with proper sandbox attributes for login functionality, loading states, and close button to dismiss after completing workflow.",
  "frontend/src/pages/KarigarDetail.tsx": "Karigar detail page that now decodes the URL-encoded karigar name from route params using `decodeURIComponent` before filtering orders, ensuring the correct pending orders are loaded when navigating from the Karigars tab.",
  "frontend/src/components/dashboard/SuppliedQtyDialog.tsx": "RB supply dialog with per-entry partial supply hints (showing how many units will remain pending) and a summary warning when any entry is a partial supply, using the fixed `useBatchSupplyRBOrders` hook that calls `supplyOrder` individually.",
  "frontend/src/components/masterdesigns/EditDesignModal.tsx": "Modal dialog for editing design mappings; deduplicates karigar dropdown options using `Array.from(new Set(...))` so each name appears exactly once, resets form state when opened with new data, and uses `useUpdateDesignMapping` with proper error extraction and toast notifications.",
  "frontend/src/components/layout/DataResetDialog.tsx": "Confirmation dialog component for data reset functionality that warns users about deleting all active orders (Pending, Ready, and Return from Hallmark status), lists affected tabs including Ready tab, and calls resetActiveOrders backend method with success/error feedback.",
  "frontend/src/pages/Reconciliation.tsx": "Reconciliation page that parses a master Excel file, correctly reading Order Type (SO/CO/RB) from column B (header \"Order T\" or \"Order Type\") via a header-scan step, displays new lines with colour-coded Order Type badges, and allows adding selected rows to Total Orders; the Missing in Master table also shows each order's existing Order Type badge.",
  "frontend/src/pages/AgeingStock.tsx": "Ageing Stock report page showing pending orders grouped by design code with colour-coded age bands; fixes the black screen by replacing CSS variable-based colour classes with explicit Tailwind colour classes (emerald/amber/red), adding `text-foreground` to all text elements, adding an `isError` state, and removing the `CardTitle` import that was unused.",
  "frontend/src/utils/ageingBadge.tsx": "Reusable AgeingBadge component that computes days since an order date (Time bigint) and renders a colour-coded pill badge — green for ≤14 days, amber for 15–29 days, red for ≥30 days."
}