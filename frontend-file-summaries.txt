{
  "frontend/src/App.tsx": "Main application component with TanStack Router; wraps `AppSidebar` and `SidebarInset` inside an explicit `flex min-h-screen w-full` div so the sidebar and main content render side-by-side on desktop without overlap, while the mobile header strip with `SidebarTrigger` remains visible only on small screens.",
  "frontend/src/components/layout/AppSidebar.tsx": "Left sidebar navigation with all menu items and Data Reset button; uses default import for `DataResetDialog` (fixing the named-export error), `collapsible=\"offcanvas\"` for mobile overlay behavior, and exports both named and default to satisfy App.tsx imports.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page that fetches all orders via `useGetAllOrders` and passes `orders` and `isError` props to `TotalOrdersTab`, `ReadyTab`, and `HallmarkTab` (fixing the missing required prop error), with compact responsive padding and tab trigger sizing for mobile.",
  "frontend/src/components/dashboard/SummaryCards.tsx": "Summary cards component that computes per-status metrics using unique order number counts (via Set deduplication) and dynamic weight (unit_weight × qty) so splits/returns/resupply never inflate the order count or show stale weights.",
  "frontend/src/components/dashboard/TotalOrdersTab.tsx": "Total Orders tab updated to use `useKarigarResolver` hook for all karigar display and search filtering — karigar is now always resolved dynamically from master design mappings at render time, never from the stored order.karigarName field; shows 'Unassigned' badge for orders with no matching design mapping.",
  "frontend/src/components/dashboard/ReadyTab.tsx": "Ready tab with grouped expandable rows, per-row Return dialog with strict qty validation, dynamic weight display (unit_weight × qty), zero-qty row filtering, unique order count; fixed `returnToPending.mutateAsync` call to use `orderId` and `returnedQty` field names matching the hook's expected interface.",
  "frontend/src/components/dashboard/HallmarkTab.tsx": "Hallmark tab with search/filter, grouped expandable rows, Mark as Returned bulk action, and Export JPEG / Export PDF (All) / Export PDF (Selected) buttons; uses default import for `DesignImageModal` and exports both named and default.",
  "frontend/src/components/dashboard/CustomerOrdersTab.tsx": "Tab displaying CO type orders with Pending status using search by order number, design code, or generic name.",
  "frontend/src/components/dashboard/KarigarsTab.tsx": "Karigar management tab updated to use `useGetAllDesignMappings` + `resolveKarigar` utility for strict dynamic karigar grouping — orders are grouped purely by master design lookup (never stored order field), 'Unassigned' group is rendered at the bottom with grey styling, and weight uses dynamic unit_weight × qty calculation.",
  "frontend/src/components/dashboard/OrderTable.tsx": "Reusable order table with bulk select, Mark as Ready, and export dropdown; updated to call `exportToPDF(orders)` and `exportToJPEG(orders)` with the new signatures (no actor argument), fixing the type mismatch errors.",
  "frontend/src/pages/IngestOrders.tsx": "Order ingestion page that uses the existing `parseOrdersExcel` utility (which returns a typed array directly) to parse Excel files, generates unique orderIds with orderNo+design+timestamp+index+random suffix to prevent duplicate key traps, converts orderDate ms to nanoseconds BigInt, processes orders sequentially, and displays clean error messages extracted from IC canister rejections.",
  "frontend/src/pages/UnmappedCodes.tsx": "Unmapped design codes page with inline editing for Generic Name and Karigar; fixed `useReassignDesign` call to include the required `movedBy: \"user\"` field.",
  "frontend/src/pages/MasterDesigns.tsx": "Master designs page; updated `EditDesignModal` usage to pass `karigars` prop (instead of the removed `availableKarigars`/`onSave`/`onAddKarigar` props), and deduplicates the karigar list before passing it to the modal.",
  "frontend/src/pages/DesignImages.tsx": "Design image upload page with optimized parallel upload processing (5 concurrent uploads), support for both folder and ZIP file uploads with proper type conversion for Uint8Array to ensure ArrayBuffer compatibility, progress tracking with current file display, automatic design code extraction from filenames, and detailed upload result reporting using ExternalBlob for backend storage.",
  "frontend/src/pages/TagPrinting.tsx": "Tag printing page with date-range filtering and design-group selection; updated to use `useGetReadyOrdersByDateRange` as a mutation (called imperatively on button click) and imports `useUpdateDesignGroupStatus` correctly.",
  "frontend/src/pages/BarcodeScanning.tsx": "Barcode scanning page with dual-mode support for hardware scanners and camera-based scanning using useQRScanner hook, automatic barcode detection with debouncing, enhanced visual feedback with gold targeting frame overlay and success animations, proper camera initialization state handling with jsQRLoaded check, automatic order ID extraction from scanned data, and batch update functionality using useBatchUpdateOrderStatus hook.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all backend operations; adds `useDesignMappingsMap`, `useKarigarResolver` (returns a dynamic resolver function using latest master design data), and updates `useGetOrdersByKarigar` to resolve karigar dynamically from master design mappings instead of the stored order field.",
  "frontend/src/utils/excelParser.ts": "Excel parsing utilities that scan row 1 headers for an 'Order Type' column (normalized, case-insensitive, ignoring spaces/underscores); if found, each data row's Order Type is read from that column and resolved to RB/SO/CO (blank or unrecognised defaults to CO); if no 'Order Type' header exists, all orders default to CO for backward compatibility. Also removes the validation error for missing/unrecognised Order Type since CO is now the valid default fallback.",
  "frontend/src/utils/exportUtils.ts": "Export utilities with fixed TypeScript types — uses `any` cast for dynamic XLSX import (no `typeof import(\"xlsx\")`), returns `ArrayBuffer` (via `.slice(0)`) from `buildPDFBytes` to avoid `Uint8Array<ArrayBufferLike>` Blob type error, and adds `exportToJPEG` legacy alias so OrderTable still compiles.",
  "frontend/src/index.css": "Global styles with OKLCH color system; fixes dark mode by raising `--muted`, `--card`, `--background`, and `--border` lightness values so skeleton/loading states are visible against the dark background. Removes the problematic `[data-state=\"active\"]` global color override and the `button:hover` scale transform that were causing invisible text and layout issues.",
  "frontend/tailwind.config.js": "Tailwind configuration with Inter and Playfair Display fonts, gold color tokens with hover variants, enhanced shadows including gold-themed shadows, and responsive container padding for mobile-first jewellery industry design.",
  "frontend/src/components/dashboard/DesignImageModal.tsx": "Modal for displaying design images with zoom/pan controls; fixed to derive `imageUrl` from `designData.blob.getDirectURL()` and use the `designCode` prop directly for the design details section, matching the updated `useGetDesignImage` hook return shape of `{ blob, genericName, karigarName }`.",
  "frontend/src/components/dashboard/UnmappedSection.tsx": "Alert section showing unmapped design codes; adds explicit `UnmappedGroup` interface and typed `reduce` accumulator to fix all TypeScript `unknown` type errors.",
  "frontend/src/components/tagprinting/WebviewModal.tsx": "Reusable webview modal component displaying MPN system in an iframe with proper sandbox attributes for login functionality, loading states, and close button to dismiss after completing workflow.",
  "frontend/src/pages/KarigarDetail.tsx": "Karigar detail page updated to fetch `useGetAllDesignMappings` and filter orders using `resolveKarigar(order.design, designMappingsMap) === decodedName` — karigar is resolved dynamically from master design at render time, never from the stored order.karigarName field; handles 'Unassigned' route parameter correctly.",
  "frontend/src/components/dashboard/SuppliedQtyDialog.tsx": "RB supply dialog that validates each entry (qty > 0, qty ≤ available) with visible error messages before calling `useBatchSupplyRBOrders`; fixed the mutation payload to use `suppliedQty` (matching the hook's expected field name) instead of `suppliedQuantity`.",
  "frontend/src/components/masterdesigns/EditDesignModal.tsx": "Modal dialog for editing design mappings; deduplicates the karigar dropdown using `Array.from(new Set(...))` so each name appears exactly once, and after saving invalidates all relevant React Query caches (allOrders, orders, masterDesigns, designMappings, uniqueKarigars, karigars) so KarigarsTab and TotalOrdersTab immediately reflect the reassignment.",
  "frontend/src/components/layout/DataResetDialog.tsx": "Confirmation dialog component for data reset functionality that warns users about deleting all active orders (Pending, Ready, and Return from Hallmark status), lists affected tabs including Ready tab, and calls resetActiveOrders backend method with success/error feedback.",
  "frontend/src/pages/Reconciliation.tsx": "Reconciliation page that parses a master Excel file, correctly reading Order Type (SO/CO/RB) from column B (header \"Order T\" or \"Order Type\") via a header-scan step, displays new lines with colour-coded Order Type badges, and allows adding selected rows to Total Orders; the Missing in Master table also shows each order's existing Order Type badge.",
  "frontend/src/pages/AgeingStock.tsx": "Ageing Stock page showing pending orders grouped by design code with age-band colour coding; fixed `SuppliedQtyDialog` prop from `rbOrders` to `orders` to match the updated component interface.",
  "frontend/src/utils/ageingBadge.tsx": "Reusable AgeingBadge component that computes days since an order date (Time bigint) and renders a colour-coded pill badge — green for ≤14 days, amber for 15–29 days, red for ≥30 days.",
  "frontend/index.html": "HTML entry point with viewport meta tag updated to include `maximum-scale=1.0, user-scalable=no` to prevent mobile browsers from requiring manual zoom and ensure the layout renders at the correct scale at 100% zoom.",
  "frontend/src/utils/karigarResolver.ts": "New utility file providing `resolveKarigar(designCode, mappingsMap)` — the single source of truth for karigar resolution that looks up design_code in master design mappings and returns 'Unassigned' if not found — plus `buildDesignMappingsMap` to convert raw API tuples into a normalized Map for O(1) lookups."
}